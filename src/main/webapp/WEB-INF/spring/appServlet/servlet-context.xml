<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"    
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"   
	xsi:schemaLocation="http://www.springframework.org/schema/mvc 
		https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd 
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">
		
		
		<!--  namespaces에서 mybatis를 추가함으로서 위에 mybatis링크가 생성됨 -->
		

	
	<!-- 어노테이션을 사용할 수 있도록 햏주는 설정(@Controller,@Service,@Autowired 등..) -->
	<!--  @Controller를 인식할 수 있는 설정추가(@Service 등...) -->
	<annotation-driven />
	
	<!--  나중에 css나 이미지등을 불러올 때 알아보자 -->
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!--  Controller의 메서드에서 "home"을 반환하면 home.jsp로 갈 수 있었던 이유 -->
	<!--  "home" -/WEB-INF/views/home.jsp -->
	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 우리가 사용하는 어노테이션을 인식할 수 있는 범위설정 -->
	<!--  com.mvc.main 하위의 패키지 및 클래스에서만 어노테이션을 인식한다. -->
	<!--  value="classpath:com/mvc/main/dao/ 가 맞는지 다시 확인하자!!!!!!!!!!! 아니라면 수정  -->
	<context:component-scan base-package="com.mvc.member" />
	
	
	<!--DB 접속 정보  -->
	<!-- 접속주소,아이디,비밀번호 꼭 확인 -->
	<beans:bean name="datasource" class="org.apache.commons.dbcp.BasicDataSource">
		<beans:property name="driverClassName" value="org.mariadb.jdbc.Driver"/>
		<beans:property name="url" value="jdbc:mariadb://localhost:3306/mydb"/>
		<beans:property name="username" value="web_user"/>
		<beans:property name="password"  value="pass"/>
	</beans:bean>
	
	
	<!-- 마이바티스 사용 설정 -->
	<!-- name = 필수요소(이름이 정해져있음) /id = 선택요소(이름이 정해져 있지 않음) -->
	<!-- 어떤 DB를 쓸건지 정보 제공 -->
	<!-- 쿼리문(xml)이 어디에 있는지 지정 -->
	<!--  mapper 위치 -->
	<!--  value="classpath:com/mvc/main/dao/ 맞는지 다시 확인하자!!!!!!!!!!!  아니라면 수정-->
	
	<beans:bean id="splSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<beans:property name="dataSource" ref="datasource"/>
		<beans:property name="mapperLocations" value="classpath:com/mvc/member/dao/*.xml"/>
	</beans:bean>
	
	
	<!-- 간편 사용 설정(마이바티스 사용하는 패키지 범위)-->
	<!-- 빈 체크여부, 패키지 정보  -->
	<!--  !!!!!value="classpath:com/mvc/main/dao/이 정말 맞는지 다시 확인하자!!!!  아니라면 수정-->
	<mybatis-spring:scan base-package="com.mvc.member.dao"/>
	
	
	
</beans:beans>